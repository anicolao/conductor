# Conductor Environment Variables

# --- UI (SvelteKit) ---
# Used in: observability-ui/
# These variables must start with PUBLIC_ to be accessible to the SvelteKit frontend in the browser.

# The GitHub OAuth App Client ID
# Usage: Used in the UI to initiate the OAuth flow and in the callback page.
# Found in: observability-ui/src/routes/+page.svelte, observability-ui/src/routes/auth/callback/+page.svelte
PUBLIC_GITHUB_CLIENT_ID=your_github_client_id_here

# The URL of the Firebase Function that handles the OAuth code exchange
# Usage: The callback page sends the authorization code to this URL to get an access token.
# Local development: http://127.0.0.1:5001/llm-orch-conductor-bridge/us-central1/githubOAuthExchange
# Production: https://us-central1-llm-orch-conductor-bridge.cloudfunctions.net/githubOAuthExchange
# Found in: observability-ui/src/routes/auth/callback/+page.svelte
PUBLIC_OAUTH_EXCHANGE_URL=http://127.0.0.1:5001/llm-orch-conductor-bridge/us-central1/githubOAuthExchange

# --- Backend (Firebase Functions) ---
# Used in: functions/index.js
# These are sensitive credentials and should be set using `firebase functions:secrets:set`.
# They ARE NOT loaded from this .env file by the Firebase Functions backend.
# Reference:
# GITHUB_CLIENT_ID=your_github_client_id_here
# GITHUB_CLIENT_SECRET=your_github_client_secret_here

# --- Conductor CLI / Core ---
# Used in: src/index.ts
# The API key for Google Gemini, used for LLM-based orchestration tasks.
GEMINI_API_KEY=your_gemini_api_key_here
