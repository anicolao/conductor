# Conductor repository instructions

- Read `/home/runner/work/conductor/conductor/README.md`, `/home/runner/work/conductor/conductor/MVP_DESIGN.md`, and `/home/runner/work/conductor/conductor/VISION.md` before acting.
- This repository implements the Conductor MVP described in those markdown files.
- The active persona is selected by the GitHub issue label `persona: conductor` or `persona: coder`.
- If no `persona:` label is present, only an explicit `@conductor` mention should start the workflow.
- Use `gh` for issue comments, labels, and pull requests.
- Keep handoffs explicit: set the next persona label first, then post the handoff comment.
- Keep changes small and visible in normal GitHub artifacts.
